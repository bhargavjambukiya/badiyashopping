package com.app.badiyashopping.data.network

interface AuthApi {

 /*   @FormUrlEncoded
    @POST("auth/login")
    suspend fun login(
        @Field("email") email: String,
        @Field("password") password: String
    ) : LoginResponse*/
}